**Compile command**

`g++-13 -fdiagnostics-color=always -std=c++20 -ld_classic -DLOCAL -g ${file} -o ${fileDirname}/${fileBasenameNoExtension}`

**Task for VS Code**

```
{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++-13 build active file",
            "command": "/opt/homebrew/bin/g++-13",
            "args": [
                "-fdiagnostics-color=always",
                "-std=c++20",
                "-ld_classic",
                "-DLOCAL",
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        },
        {
            "label": "run",
            "type": "shell",
            "command": "./${fileBasenameNoExtension} < input.txt > output.txt",
            "group": {
                "kind": "test",
                "isDefault": true
            },
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            },
            "dependsOn": "build",
            "options": {
                "cwd": "${fileDirname}",
            },
            "problemMatcher": []
        }
    ],
    "version": "2.0.0"
}

```